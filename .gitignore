# =============================================================================
# 抖音画质助手 - 仓库排除规则
# =============================================================================

# -----------------------------------------------------------------------------
# 1) 抖音官方文件原文 —— 绝对不能提交
#    这些是 ByteDance 的版权代码。它们会在每台机器上首次运行时，从本机安装的
#    抖音自动生成（deploy.ps1 / GUI 都会固化一份原始副本），所以仓库里不需要。
# -----------------------------------------------------------------------------
backup/

# -----------------------------------------------------------------------------
# 2) 开发/侦察过程产物 —— 含从抖音客户端解包出来的第三方代码，禁止提交
# -----------------------------------------------------------------------------
work/

# -----------------------------------------------------------------------------
# 3) 本机运行时状态 —— 因机器而异，无分发价值
# -----------------------------------------------------------------------------
config.json
*.log
hook-active.marker
gui-error.txt

# -----------------------------------------------------------------------------
# 4) 依赖与构建中间产物
# -----------------------------------------------------------------------------
node_modules/
*.tmp
~$*

# -----------------------------------------------------------------------------
# 5) 系统与编辑器杂物
# -----------------------------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.swp

# -----------------------------------------------------------------------------
# 显式保留：版本清单与更新日志必须入库
# -----------------------------------------------------------------------------
!versions.json
!CHANGELOG.md
